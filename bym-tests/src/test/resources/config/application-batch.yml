spring:
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
server:
  port: 8080
bym:
  batch:
    launcher:
      mode: service
mal:
  api-version: "v1"
  client-header: "X-MAL-CLIENT-ID-TEST"
  # url placeholder, will be overrided in tests to use test containers mockserver
  url: "https://api.myanimelist-test.net"
  client-id: 'custom-client-id-1234'